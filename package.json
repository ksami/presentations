{
  "scripts": {
    "build": "npx reveal-md slides/ --static _static --listing-template templates/listing.html --highlight-theme atom-one-dark --absolute-url https://ksami.github.io/presentations",
    "watch": "watch \"npm run build\" slides",
    "deploy": "npm run build && gh-pages -d _static"
  },
  "dependencies": {
    "gh-pages": "^2.0.1",
    "js-yaml": ">=3.13.1",
    "reveal-md": "^5.4.0",
    "ws": ">=3.3.1"
  },
  "devDependencies": {
    "watch": "^1.0.2"
  }
}

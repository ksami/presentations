{
  "scripts": {
    "build": "reveal-md slides/ --static _static --listing-template templates/listing.html --highlight-theme atom-one-dark --absolute-url https://ksami.github.io/presentations",
    "watch": "watch \"npm run build\" slides",
    "deploy": "npm run build && gh-pages -d _static"
  },
  "dependencies": {
    "gh-pages": "^2.0.1",
    "js-yaml": ">=3.13.1",
    "reveal-md": "^3.1.0",
    "ws": ">=7.4.6"
  },
  "devDependencies": {
    "watch": "^1.0.2"
  }
}
